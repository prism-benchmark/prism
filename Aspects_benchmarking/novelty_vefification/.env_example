#!/bin/bash
# ============================================================================
# DEPRECATED — Use the root .env.example instead:
#   cp ../../.env.example ../../.env
#
# All AI configuration is now centralized in ai_config.py at the repo root.
# This file is kept for backward compatibility only.
# ============================================================================
